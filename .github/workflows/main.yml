on:
  push:
    branches:
      - master
    








jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: "test"
        run: pwd
      - name: "test2"
        run: echo mohamed
      